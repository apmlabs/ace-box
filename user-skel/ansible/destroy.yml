---
- hosts: localhost
  connection: local
  become: true
  become_user: "{{ ace_box_user | default(lookup('env','USER')) }}"
  vars:
    ace_config_file_path: "./ace.config.yml"
  tasks:
    # - include_tasks:
    #     file: roles/monaco/tasks/destroy.yml
